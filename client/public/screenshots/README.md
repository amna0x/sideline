Drop your screenshots here and name them `screenshot1.png`, `screenshot2.png`, etc.

Helpful commands (macOS / Linux):

```bash
# resize and convert to webp
cwebp -q 80 input.png -o screenshot1.webp
# or resize with imagemagick
magick input.png -resize 720x1280\> screenshot1.png
```

When ready, replace the `.placeholder` files with the actual images (use same file names).